# Hardware / IO
pyserial==3.5
pynmea2==1.19.0
smbus2==0.4.3
lgpio==0.2.2.0
pygnssutils==1.0.30

# Math / config
# numpy>=2.1 / pyproj>=3.7 are the first versions with Python 3.13 wheels.
numpy>=2.1,<3
pyproj>=3.7,<4
pyyaml>=6.0.2,<7

# Web UI (FastAPI + WebSocket, uvicorn with uvloop for the Pi)
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.0.1

# Host telemetry for the UI status panel (CPU / RAM / temp / uptime)
psutil>=5.9
